export interface FAQItem {
  question: string;
  answer: string;
}

export interface TimelinePhase {
  title: string;
  days: string;
  description: string;
}

export interface Testimonial {
  name: string;
  role: string;
  title: string;
  description: string;
  quote: string;
  image: string;
}